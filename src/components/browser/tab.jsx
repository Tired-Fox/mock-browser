import { h, Fragment } from 'preact';
import { useState } from 'preact/hooks';
import './browser.scss';

export function Tab({ ...props }) {
    // URL, Title, Favicon
    return (
        <>
        </>
    );
}